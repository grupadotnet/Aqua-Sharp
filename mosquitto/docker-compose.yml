version: "3.8"

services:
  mosquitto:
    image: eclipse-mosquitto
    volumes:
      - ./conf:/mosquitto/config
      - ./data:/mosquitto/data
      - ./log:/mosquitto/log
    environment:
      - PUID=1883 
      - PGID=1883